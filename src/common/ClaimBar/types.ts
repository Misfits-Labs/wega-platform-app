import { styled } from 'twin.macro';

export const Count = styled.span`
 display: inline-block;
 padding: 7.5px 7.5px;
 font-size: 28px;
 font-style: normal;
 font-weight: 500;
 line-height: 19px;
 color: #FDFDFD;
 border: unset;
`